import 'package:flutter/material.dart';

class UserEntity {
  final int? id;
  final String? username;
  final String? fullname;
  final Color? color;

  UserEntity({this.id, this.username, this.fullname, this.color});
}
